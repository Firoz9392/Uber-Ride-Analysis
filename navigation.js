document.addEventListener('DOMContentLoaded', () => { const nav = document.querySelector('#sidebar-nav'); if (!nav) return; ['Admin Data', 'Forecast', 'Alerts', 'Live Feed'].forEach(label => { const links = [...nav.querySelectorAll('a')].filter(link => link.textContent.trim().toLowerCase() === label.toLowerCase()); links.slice(1).forEach(link => link.remove()); }); nav.querySelectorAll('a').forEach(link => { link.style.display = 'flex'; }); });
